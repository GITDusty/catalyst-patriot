---
phase: 01-core-visualization-(mvp)
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - app/components/BudgetExplorer.tsx
  - app/page.tsx
  - app/globals.css
autonomous: true

must_haves:
  truths:
    - "Landing page shows FY 2024-25 context, totals, and data source attribution"
    - "Users can explore categories and open drill-downs on mobile"
    - "Charts have visible, linked text alternatives on the page"
  artifacts:
    - path: "app/components/BudgetExplorer.tsx"
      provides: "Client-side state + composition of chart, table, and dialog"
    - path: "app/page.tsx"
      provides: "Page layout with headers, attribution, and explorer component"
    - path: "app/globals.css"
      provides: "Theme variables and chart color tokens"
  key_links:
    - from: "app/components/BudgetExplorer.tsx"
      to: "app/components/charts/BudgetDonut.tsx"
      via: "data + onSelectCategory"
      pattern: "BudgetDonut"
    - from: "app/components/BudgetExplorer.tsx"
      to: "app/components/tables/CategoryTable.tsx"
      via: "tableId and aria-describedby"
      pattern: "CategoryTable"
---

<objective>
Compose the landing page experience with responsive layout, attribution, and interactive exploration.

Purpose: Deliver the MVP page that meets accessibility, mobile, and attribution requirements.
Output: Home page layout, client explorer component, and updated theme tokens.
</objective>

<execution_context>
@/Users/dustanbruggeman/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dustanbruggeman/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-visualization-(mvp)/01-RESEARCH.md
@app/page.tsx
@app/globals.css
@app/data/florida-budget.ts
@app/components/charts/BudgetDonut.tsx
@app/components/modals/CategoryDialog.tsx
@app/components/tables/CategoryTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the BudgetExplorer client component</name>
  <files>app/components/BudgetExplorer.tsx</files>
  <action>
    Create a client component that:
    - Imports Florida budget data and formatting helpers.
    - Manages selected category state for drill-down.
    - Renders the donut chart, category table, and dialog together.
    - Generates a stable `tableId` and wires it to `aria-describedby` on the chart.
    - Surfaces fiscal year and total budget summary near the chart.

    Keep the layout responsive: stack on mobile, split columns on larger screens.
  </action>
  <verify>npm run lint</verify>
  <done>Explorer composes chart + table + dialog with working selection state.</done>
</task>

<task type="auto">
  <name>Task 2: Update the home page with context and attribution</name>
  <files>app/page.tsx</files>
  <action>
    Update the home page to:
    - Present a clear headline and short intro describing the FY 2024-25 Florida budget.
    - Render `BudgetExplorer` as the primary interactive section.
    - Display data source attribution and fiscal year in a dedicated metadata block.

    Keep `page.tsx` as a server component and keep state inside `BudgetExplorer`.
  </action>
  <verify>npm run lint</verify>
  <done>Home page includes context, explorer UI, and attribution text.</done>
</task>

<task type="auto">
  <name>Task 3: Define theme tokens and chart palette</name>
  <files>app/globals.css</files>
  <action>
    Update global styles to establish a modern, patriotic palette and chart tokens:
    - Add CSS variables for chart colors (`--chart-1`..`--chart-6`).
    - Ensure background/foreground contrast meets accessibility.
    - Set body font to the project sans variable instead of the default stack.
    - Add utility classes or base styles to support section spacing and layout.

    Avoid neon or overly saturated colors and avoid hover-only affordances.
  </action>
  <verify>npm run lint</verify>
  <done>Theme tokens exist and chart components can reference palette variables.</done>
</task>

</tasks>

<verification>
- npm run lint
</verification>

<success_criteria>
- Landing page communicates FY 2024-25 context, totals, and data source attribution.
- Interactive exploration works on mobile with click/tap drill-downs.
- Text alternatives are visible and linked from charts for accessibility.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-visualization-(mvp)/01-03-SUMMARY.md`
</output>
